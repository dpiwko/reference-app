server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /health.txt {
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
